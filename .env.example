# loregist 환경변수 예시
# 이 파일을 .env 로 복사하고 실제 값을 설정하세요.
# cp .env.example .env

# PostgreSQL 비밀번호
LOREGIST_DB_PASSWORD=vector_local

# 작업 루트 디렉터리 (절대경로)
LOREGIST_WORKSPACE=~/workspace

# 기업망 SSL inspection 우회 — 1: 활성(기본), 0: 비활성
# LOREGIST_NO_SSL_VERIFY=1
